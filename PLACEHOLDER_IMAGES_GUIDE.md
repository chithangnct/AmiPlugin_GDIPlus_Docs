# 🎨 Hướng Dẫn Tạo Hình Ảnh Mẫu / Placeholder Images Guide

## Tiếng Việt

### Bước 1: Mở Trình Tạo Hình Ảnh

1. Mở file `image-generator.html` bằng trình duyệt web (Chrome, Firefox, Edge)
2. Hoặc double-click vào file `image-generator.html`

### Bước 2: Tạo Hình Ảnh

1. Trang web sẽ **tự động tạo tất cả 31 hình ảnh** khi vừa mở
2. Bạn sẽ thấy thanh tiến trình và các thẻ hình ảnh xuất hiện
3. Chờ đến khi hiển thị: "✅ Successfully generated all 31 images!"

### Bước 3: Tải Xuống

**Cách 1: Tải tất cả một lần (Khuyến nghị)**
1. Click nút **"Download All as ZIP"**
2. Lưu file `GDIPlus_PlaceholderImages.zip`
3. Giải nén file ZIP
4. Copy tất cả file `.png` vào thư mục `images/examples/`

**Cách 2: Tải từng hình**
1. Scroll xuống xem các hình ảnh đã tạo
2. Click nút **"Download"** dưới mỗi hình
3. Lưu từng file vào `images/examples/`

### Bước 4: Kiểm Tra

1. Mở file `index.html` của documentation
2. Click vào nút **"Hình minh họa"** ở góc phải mỗi thẻ function
3. Thẻ sẽ lật và hiển thị hình ảnh mẫu

### Bước 5: Thay Thế (Sau này)

Khi bạn có hình ảnh thật từ AmiBroker:
1. Tạo screenshot của function
2. Đặt tên file giống tên function (ví dụ: `GDIpPieChart.png`)
3. Thay thế file cũ trong `images/examples/`

---

## English

### Step 1: Open Image Generator

1. Open `image-generator.html` in your web browser (Chrome, Firefox, Edge)
2. Or simply double-click the `image-generator.html` file

### Step 2: Generate Images

1. The page will **automatically generate all 31 images** when opened
2. You'll see a progress bar and image cards appearing
3. Wait until it shows: "✅ Successfully generated all 31 images!"

### Step 3: Download

**Method 1: Download All at Once (Recommended)**
1. Click **"Download All as ZIP"** button
2. Save the `GDIPlus_PlaceholderImages.zip` file
3. Extract the ZIP file
4. Copy all `.png` files to `images/examples/` folder

**Method 2: Download Individually**
1. Scroll down to see generated images
2. Click **"Download"** button under each image
3. Save each file to `images/examples/`

### Step 4: Verify

1. Open the documentation `index.html`
2. Click the **"View Image"** button in the top-right of any function card
3. The card will flip and show the placeholder image

### Step 5: Replace (Later)

When you have real images from AmiBroker:
1. Take a screenshot of the function output
2. Name the file exactly as the function name (e.g., `GDIpPieChart.png`)
3. Replace the old file in `images/examples/`

---

## 📋 Danh Sách Hình Ảnh / Image List

Tổng cộng **31 hình ảnh** / Total **31 images**:

1. GDIpResetDefaults.png
2. GDIpClearCache.png
3. GDIpSelectBrushColor.png
4. GDIpSelectPenColor.png
5. GDIpSelectLinearGradientBrush.png
6. GDIpSelectRadialGradientBrush.png
7. GDIpSelectSliceGradientBrush.png
8. GDIpSelectLinearGradientColor.png
9. GDIpSelectFont.png
10. GDIpRectangle.png
11. GDIpRoundRect.png
12. GDIpCircle.png
13. GDIpEllipse.png
14. GDIpLine.png
15. GDIpPolygon3.png
16. GDIpPolygon4.png
17. GDIpPolygon5.png
18. GDIpPolygon6.png
19. GDIpText.png
20. GDIpDrawText.png
21. GDIpSmoothLine.png
22. GDIpSmoothLineXY.png
23. GDIpAreaChart.png
24. GDIpPieChart.png
25. GDIpDonutChart.png
26. GDIpGaugeChart.png
27. GDIpProcessCircleChart.png
28. GDIpRadarChart.png
29. GDIpArc.png
30. GDIpPlotFlow.png
31. GDIpDrawSVG.png

---

## 🎨 Thông Số Hình Ảnh / Image Specifications

- **Định dạng / Format**: PNG
- **Kích thước / Size**: 800x600 pixels
- **Màu sắc / Colors**: Gradient tự động theo loại function / Auto-gradient by function type
- **Nội dung / Content**:
  - Tên function / Function name
  - "GDIPlus Function" subtitle
  - Hình dạng minh họa / Decorative shapes based on function type

---

## 🔧 Xử Lý Sự Cố / Troubleshooting

### Hình ảnh không hiển thị / Images not showing
- Kiểm tra file có đúng tên không / Check file names match exactly
- Kiểm tra file trong thư mục `images/examples/` / Verify files are in `images/examples/`
- Xóa cache trình duyệt và reload / Clear browser cache and reload

### Không tải được ZIP / Cannot download ZIP
- Thử tải từng hình riêng lẻ / Try downloading images individually
- Kiểm tra trình duyệt có cho phép tải file / Check browser download permissions

### Thẻ không lật / Card doesn't flip
- Kiểm tra file `script.js` đã load / Verify `script.js` is loaded
- Mở Console (F12) xem lỗi / Open Console (F12) to check errors

---

## 💡 Mẹo / Tips

1. **Tạo lại mọi lúc**: Bạn có thể mở `image-generator.html` bất cứ lúc nào để tạo lại hình
2. **Tùy chỉnh**: Sau này có thể chỉnh sửa file `image-generator.html` để thay đổi màu sắc/kiểu dáng
3. **Backup**: Nên backup các hình ảnh thật sau khi thay thế placeholder
4. **Kích thước**: Hình thật nên có tỷ lệ tương tự (4:3 hoặc 16:9) để hiển thị đẹp

---

**Chúc bạn thành công! / Good luck! 🎉**
